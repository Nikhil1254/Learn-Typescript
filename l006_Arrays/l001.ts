//1. it will be of type never we can't assign or push values in it
let friends = [];
// friends.push("Nikhil")  error


//2. first way to define array
let heros: string[] = ["Thor", "Iron Man"];
heros.push('Hulk');

//3. another way to create arrays
let powers: Array<number> = [1, 2, 3];

powers.push(4);
